% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{logBF_summary_cpp}
\alias{logBF_summary_cpp}
\title{Compute log10 Bayes Factor for a single model}
\usage{
logBF_summary_cpp(XtY, XtX, YtY, sigma_vec, gamma_idx, n)
}
\arguments{
\item{XtY}{Numeric vector of length \eqn{d}. Pre-computed
\eqn{X^\top Y = \texttt{betaX}^\top \texttt{betaY}}}

\item{XtX}{Numeric matrix of dimension \eqn{d \times d}. Pre-computed
\eqn{X^\top X = \texttt{betaX}^\top \texttt{betaX}}}

\item{YtY}{Positive numeric scalar. Pre-computed
\eqn{Y^\top Y = \texttt{betaY}^\top \texttt{betaY}}}

\item{sigma_vec}{Numeric vector of length \eqn{d}. Prior standard
deviations for causal effects, one per exposure}

\item{gamma_idx}{Integer vector of column indices into betaX identifying the exposures in model \eqn{\gamma}}

\item{n}{Positive integer. Number of SNPs (instruments) \eqn{n}}
}
\value{
Scalar double: log10 Bayes Factor (BF) for model \eqn{\gamma}. Returns
\code{-Inf} if the system is (near-)singular
}
\description{
Evaluates the log10 Bayes Factor (BF) for a model defined by the
indicator vector \code{gamma_idx} using pre-computed summary-level statistics \eqn{X^\top X}, \eqn{X^\top Y} and \eqn{Y^\top Y}. This
avoids re-multiplying the full data matrices for every model evaluation and is the innermost computational kernel called by
\code{exhaustive_bf_cpp} and \code{sss_core_cpp}.
Throughout, \eqn{d} denotes the number of exposures, \eqn{n} the number of SNPs.
}
